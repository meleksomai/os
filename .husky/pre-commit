# Formats and lints the staged files with the workspace's pinned Ultracite,
# the same version CI runs (`pnpm check`). lint-staged stashes unstaged
# changes, runs the command on staged files only, and re-stages the result.
pnpm exec lint-staged
